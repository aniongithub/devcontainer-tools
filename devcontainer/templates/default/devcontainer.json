{
	"name": "${DEVCONTAINER_NAME}",
	"context": "..",
	"workspaceFolder": "/workspace/",
	"dockerComposeFile": "docker-compose.yml",
	"service": "${DEVCONTAINER_NAME}-${DEVCONTAINER_ID}-devcontainer",
	"shutdownAction": "${DEVCONTAINER_SHUTDOWN_ACTION}",
    "settings": { "terminal.integrated.shell.linux": "${DEVCONTAINER_SHELL}" }
}